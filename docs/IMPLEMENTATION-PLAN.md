# Local launch prototype

Goal: build the authorized first Astro draft and inspect it in an isolated Chrome DevTools MCP session.

Architecture: static Astro; individual hero, portrait, proof, timeline, collaboration and contact components. Shared CSS tokens and local content data. No client framework.

1. Bootstrap Astro and self-host IBM Plex Sans / Mono. Configure static Netlify output.
2. Implement the approved single-page sequence, with email as primary action and chronological CV-grounded entries.
3. Run the build and localhost server. Launch Chrome DevTools MCP with `--isolated` through a local SDK client.
4. Capture desktop and mobile screenshots; inspect overflow, semantics, navigation, email drafts, and console errors. Patch visible issues and capture final evidence.
5. Record the session delta and evidence map. Keep workshop source documents read-only.

Source precedence: ../../PRODUCT.md, updated design spec, recovered transcript, benchmark notes. Exact metrics remain unverified and are not published. Calendar destination is not supplied; calls are explicitly arranged by email. GitHub profile portrait is provisional. Local preview is noindex until publication review.
